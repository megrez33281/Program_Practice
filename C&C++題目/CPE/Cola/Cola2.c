#include stdio.h
#include string.h
#include stdlib.h
#include math.h
#include stdbool.h

int main(){
	int N;
	while(scanf(%d,&N) != EOF){
		int bororrow = N2; the most amount yout can bororrow
		printf(%dn, N + N2);
	}

	return 0;
}